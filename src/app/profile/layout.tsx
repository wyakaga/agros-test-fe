import { Metadata } from "next";

import ProfileGuarded from "./page";

export const metadata: Metadata = {
  title: "AGROS | Profil",
  description: "Halaman profil pengguna AGROS",
};

export default ProfileGuarded;
